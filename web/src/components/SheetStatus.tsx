"use client";
import { useEffect, useState } from "react";
export function SheetStatus() { const [rows, setRows] = useState<string[][]>([]); const [classRows, setClassRows] = useState<string[][]>([]); const [error, setError] = useState(""); useEffect(() => { fetch("/api/sheets/rows", { cache: "no-store" }).then(async (response) => ({ response, result: await response.json() as { rows?: string[][]; classRows?: string[][]; error?: string } })).then(({ response, result }) => { if (!response.ok) setError(result.error ?? "현황을 불러오지 못했습니다."); else { setRows(result.rows ?? []); setClassRows(result.classRows ?? []); } }); }, []); const table = (data: string[][]) => <div className="table-wrapper"><table className="status-table"><thead><tr><th>학번</th><th>이름</th><th>1교시</th><th>2교시</th></tr></thead><tbody>{data.map((row, index) => <tr key={`${row[0]}-${index}`}><td>{row[0]}</td><td>{row[1]}</td><td>{row[4] || "-"}</td><td>{row[7] || "-"}</td></tr>)}</tbody></table></div>; return <>{error && <div className="card"><p className="error-message">{error}</p></div>}<div className="card"><h3>1-8반(1801~1816) 실시간 이석 명단</h3>{classRows.length ? table(classRows) : <p>표시할 학생이 없습니다.</p>}</div><div className="card"><h3>📊 실시간 이석 명단</h3>{rows.length ? table(rows) : <p>표시할 데이터가 없습니다.</p>}</div></>; }
